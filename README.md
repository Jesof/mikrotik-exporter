# MikroTik Prometheus Exporter

Prometheus exporter для сбора метрик с MikroTik роутеров через RouterOS API.

## Протокол

Этот проект реализует протокол MikroTik RouterOS API на основе публично документированной спецификации:

- [MikroTik Wiki: API](https://wiki.mikrotik.com/wiki/Manual:API)

Реализация протокола написана с нуля специально для этого проекта и не содержит кода, скопированного из других реализаций.

## Возможности

- ✅ Сбор метрик с нескольких MikroTik роутеров одновременно
- ✅ Экспорт метрик в формате Prometheus
- ✅ Асинхронная архитектура на базе Tokio
- ✅ Настройка через переменные окружения или JSON
- ✅ Health check endpoint с проверкой доступности роутеров
- ✅ Периодический фоновой сбор метрик с кэшированием
- ✅ Кэширование неизменяемых метрик (system info, board name)
- ✅ Graceful shutdown (Ctrl+C) для корректного завершения фоновых задач
- ✅ Модульная архитектура с переиспользуемой библиотекой (`lib.rs`)
- ✅ Пул соединений с автоматической очисткой и управлением состоянием
- ✅ Экспоненциальная задержка при повторных подключениях (backoff)
- ✅ Rust Edition 2024 с современными практиками разработки
- ✅ Продвинутая обработка ошибок с `thiserror`
- ✅ Prelude модуль для удобного импорта типов
- ✅ Детальное trace-логирование для отладки протокола и соединений

## Собираемые метрики

### Интерфейсы

- `mikrotik_interface_rx_bytes` - Полученные байты
- `mikrotik_interface_tx_bytes` - Отправленные байты
- `mikrotik_interface_rx_packets` - Полученные пакеты
- `mikrotik_interface_tx_packets` - Отправленные пакеты
- `mikrotik_interface_rx_errors` - Ошибки приёма
- `mikrotik_interface_tx_errors` - Ошибки передачи
- `mikrotik_interface_running` - Статус интерфейса (1=работает, 0=не работает)

### Система

- `mikrotik_system_cpu_load` - Загрузка CPU (%)
- `mikrotik_system_free_memory_bytes` - Свободная память (байты)
- `mikrotik_system_total_memory_bytes` - Общая память (байты)
- `mikrotik_system_info` - Информация о системе (version, board)
- `mikrotik_system_uptime_seconds` - Аптайм системы (секунды)

Все метрики имеют лейблы `router` (имя роутера) и `interface` (имя интерфейса, где применимо).

### Сервисные метрики

- `mikrotik_scrape_success` - Количество успешных циклов сбора по роутеру
- `mikrotik_scrape_errors` - Количество ошибок сбора по роутеру
- `mikrotik_scrape_duration_milliseconds` - Длительность последнего сбора метрик (миллисекунды)
- `mikrotik_scrape_last_success_timestamp_seconds` - Unix timestamp последнего успешного сбора
- `mikrotik_connection_consecutive_errors` - Количество последовательных ошибок подключения
- `mikrotik_connection_pool_size` - Общее количество соединений в пуле
- `mikrotik_connection_pool_active` - Количество активных соединений в пуле

## Установка

### Docker

```bash
docker pull ghcr.io/jesof/mikrotik-exporter:latest

docker run -p 9090:9090 \
  -e ROUTERS_CONFIG='[{"name":"main","address":"192.168.88.1:8728","username":"admin","password":"password"}]' \
  ghcr.io/jesof/mikrotik-exporter:latest
```

### Kubernetes

См. [DEPLOYMENT.md](DEPLOYMENT.md) для подробной инструкции по развертыванию в Kubernetes.

Быстрый старт:

```bash
# Применить все манифесты
kubectl apply -k k8s/

# Или поэтапно
kubectl apply -f k8s/namespace.yaml
kubectl apply -f k8s/secret.yaml
kubectl apply -f k8s/deployment.yaml
kubectl apply -f k8s/service.yaml
```

### Из исходников

```bash
git clone https://github.com/Jesof/mikrotik-exporter.git
cd mikrotik-exporter
cargo build --release
```

Бинарный файл будет находиться в `target/release/mikrotik-exporter`

## Конфигурация

Создайте `.env` файл на основе `.env.example`:

```bash
cp .env.example .env
```

### Вариант 1: Один роутер (простая конфигурация)

```env
SERVER_ADDR=0.0.0.0:9090
ROUTEROS_ADDRESS=192.168.88.1:8728
ROUTEROS_USERNAME=admin
ROUTEROS_PASSWORD=mypassword
RUST_LOG=info
COLLECTION_INTERVAL_SECONDS=30
```

### Вариант 2: Несколько роутеров (JSON конфигурация)

```env
SERVER_ADDR=0.0.0.0:9090
ROUTERS_CONFIG=[{"name":"office","address":"192.168.88.1:8728","username":"admin","password":"pass1"},{"name":"home","address":"192.168.1.1:8728","username":"admin","password":"pass2"}]
RUST_LOG=info
```

## Запуск

```bash
./target/release/mikrotik-exporter
```

Или в режиме разработки:

```bash
cargo run
```

### Уровни логирования

Установите переменную `RUST_LOG` для управления уровнем логирования:

```bash
# Минимальное логирование (только ошибки и важные события)
RUST_LOG=error ./mikrotik-exporter

# Стандартное логирование (рекомендуется)
RUST_LOG=info ./mikrotik-exporter

# Подробное логирование для отладки
RUST_LOG=debug ./mikrotik-exporter

# Детальное trace-логирование протокола RouterOS и соединений
RUST_LOG=trace ./mikrotik-exporter
```

Уровень `trace` полезен для отладки проблем с подключением, показывает:

- Установку TCP соединений
- Обмен данными по протоколу RouterOS
- Детали аутентификации
- Состояние пула соединений
- Подробную информацию о собранных метриках

## API Endpoints

### Health Check

`GET /health` - Health check endpoint с проверкой состояния роутеров

Возвращает JSON с информацией о статусе сервиса и каждого роутера:

```json
{
  "status": "healthy",
  "version": "0.1.1",
  "routers": [
    {
      "name": "office",
      "status": "healthy",
      "consecutive_errors": 0,
      "has_successful_scrape": true
    },
    {
      "name": "home",
      "status": "degraded",
      "consecutive_errors": 3,
      "has_successful_scrape": false
    }
  ]
}
```

**Статусы:**

- `healthy` - роутер работает нормально (есть успешные сборы метрик)
- `degraded` - роутер недоступен или имеет ошибки
- `unknown` - сбор метрик еще не производился

**HTTP коды:**

- `200 OK` - все роутеры работают нормально
- `503 Service Unavailable` - один или несколько роутеров недоступны

### Metrics

`GET /metrics` - Prometheus метрики (кэшированные данные фонового сбора)

## Конфигурация Prometheus

Добавьте в `prometheus.yml`:

```yaml
scrape_configs:
  - job_name: "mikrotik"
    static_configs:
      - targets: ["localhost:9090"]
    scrape_interval: 30s
```

Если `COLLECTION_INTERVAL_SECONDS` больше чем `scrape_interval`, часть скрапов отдаст те же значения (интервалы можно синхронизировать).

## Grafana Dashboard

Готовый dashboard для Grafana доступен в `grafana/dashboard.json`.

**Импорт:**

1. Откройте Grafana → Dashboards → Import
2. Загрузите файл `grafana/dashboard.json`
3. Выберите Prometheus datasource
4. Нажмите Import

**Dashboard включает:**

- Системная информация роутера (версия, модель)
- Загрузка CPU и использование памяти
- Uptime системы
- Статус сбора метрик
- Длительность сбора метрик (Scrape Duration)
- Время последнего успешного сбора
- Количество последовательных ошибок подключения
- Статистика использования пула соединений
- Процент успешных сборов (Success Rate)
- Графики трафика по интерфейсам (RX/TX)
- Графики пакетов (RX/TX)
- Ошибки на интерфейсах
- Таблица статусов интерфейсов

См. [DEPLOYMENT.md](DEPLOYMENT.md#импорт-dashboard-в-grafana) для альтернативных способов импорта.

## Требования к MikroTik

- RouterOS с включенным API (порт 8728)
- Пользователь с правами на чтение:
  - `/interface`
  - `/system/resource`

### Включение API на MikroTik

```shell
/ip service
set api address=0.0.0.0/0 disabled=no port=8728
```

### Создание пользователя для мониторинга

```shell
/user group add name=monitoring policy=api,read
/user add name=prometheus group=monitoring password=yourpassword
```

## Разработка

### Структура проекта

```text
src/
├── lib.rs               # Публичная библиотека крейта
├── main.rs              # Точка входа приложения
├── error.rs             # Типы ошибок (с thiserror)
├── prelude.rs           # Prelude модуль для удобного импорта
├── api/
│   ├── mod.rs           # HTTP API setup и AppState
│   └── handlers/
│       ├── mod.rs
│       ├── health.rs    # Health check handler
│       └── metrics.rs   # Metrics handler
├── collector/
│   ├── mod.rs           # Оркестрация фонового сбора метрик
│   └── cleanup.rs       # Очистка пула соединений
├── config/
│   ├── mod.rs           # Загрузка конфигурации
│   └── tests.rs         # Тесты конфигурации
├── metrics/
│   ├── mod.rs           # Публичные экспорты
│   ├── labels.rs        # Типы меток для Prometheus
│   ├── parsers.rs       # Парсеры (uptime и др.)
│   ├── registry.rs      # Prometheus метрики и реестр
│   └── tests.rs         # Тесты метрик
└── mikrotik/
    ├── mod.rs           # Публичные экспорты
    ├── types.rs         # Структуры данных (InterfaceStats, SystemResource)
    ├── connection.rs    # Низкоуровневый RouterOS API (протокол, парсинг)
    ├── pool.rs          # Пул соединений с управлением состоянием
    └── client.rs        # Высокоуровневый клиент MikroTikClient
```

### Использование как библиотеки

Проект можно использовать как библиотеку в других Rust проектах:

```rust
use mikrotik_exporter::prelude::*;

#[tokio::main]
async fn main() -> Result<()> {
    let config = Config::from_env();
    let metrics = MetricsRegistry::new();
    // ... ваш код
    Ok(())
}
```

### Запуск тестов

```bash
cargo test
```

### Проверка форматирования

```bash
cargo fmt --check
cargo clippy
```

## Лицензия

MIT

## Статус реализации

### ✅ Реализовано

**Протокол и подключение:**

- ✅ Challenge-response `/login` аутентификация
- ✅ Команды `/interface/print` и `/system/resource/print`
- ✅ Парсинг RouterOS API ответов (`!re`, `!done`, `!trap`)
- ✅ Таймауты и автоматические повторные попытки
- ✅ Пул соединений с переиспользованием подключений к роутерам
- ✅ Автоматическая очистка неактивных соединений
- ✅ Экспоненциальная задержка при повторных попытках подключения
- ✅ Отслеживание последовательных ошибок подключения к роутерам

**Метрики:**

- ✅ Фоновый сбор метрик с расчётом дельт для счетчиков
- ✅ Сервисные метрики успехов и ошибок сбора
- ✅ Метрики длительности сбора и времени последнего успешного сбора
- ✅ Метрики использования пула соединений

**Архитектура и код:**

- ✅ Rust Edition 2024
- ✅ Модульная архитектура с чистым API
- ✅ Современная обработка ошибок с `thiserror`
- ✅ Prelude модуль для удобства использования
- ✅ Переиспользуемая библиотека (`lib.rs`)
- ✅ Полное покрытие тестами

**Развертывание:**

- ✅ Docker образ с multi-stage сборкой
- ✅ Манифесты для Kubernetes (Deployment, Service, ConfigMap, Secret)
- ✅ ServiceMonitor для Prometheus Operator
- ✅ Готовый dashboard для Grafana
- ✅ GitHub Actions для автоматической сборки образов

### 🚧 План улучшений

**Метрики:**

- ⏳ Дополнительные метрики системы (temperature, voltage, PSU status)
- ⏳ Метрики беспроводных интерфейсов (wireless clients, signal strength, noise)
- ⏳ Метрики очередей (queue statistics)
- ⏳ Метрики DHCP (leases, pools)
- ⏳ Метрики firewall (connections, rules hits)
- ⏳ Метрики BGP/OSPF (peers, routes)

**Протокол и безопасность:**

- ⏳ Поддержка TLS/SSL для RouterOS API (порт 8729)
- ⏳ Поддержка API-SSL сертификатов
- ⏳ Расширенные тесты парсера протокола RouterOS

**Функциональность:**

- ⏳ Динамическая перезагрузка конфигурации без рестарта
- ⏳ Метрики по группам интерфейсов (bridge, vlan, pppoe)
- ⏳ Экспорт логов RouterOS в структурированном формате
- ⏳ Поддержка custom метрик через конфигурацию

**Производительность:**

- ✅ Кэширование неизменяемых метрик (system info, board name)
- ⏳ Оптимизация парсинга больших ответов RouterOS
- ⏳ Батчинг запросов к роутерам

**Мониторинг и отладка:**

- ✅ Детальное логирование с trace level для отладки протокола
- ✅ Health check с проверкой доступности роутеров
- ⏳ Prometheus метрики самого экспортера (memory, CPU usage)

## Автор

Jesof <jesof@me.com>
